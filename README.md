# basehub mutation api example

Add stuff to BaseHub via `basehub().mutation()`.

IMPORTANT: Requires a BaseHub Admin Token.

See how data gets added here: https://basehub.com/basehub/mutation-api-playground
